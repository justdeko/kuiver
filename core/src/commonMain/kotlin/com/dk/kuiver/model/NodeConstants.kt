package com.dk.kuiver.model

import androidx.compose.ui.unit.dp

/**
 * Default node size when dimensions are not specified or measured.
 * Used across layout algorithms, rendering, and bounds calculations.
 */
internal const val DEFAULT_NODE_SIZE = 60f
internal val DEFAULT_NODE_SIZE_DP = DEFAULT_NODE_SIZE.dp
