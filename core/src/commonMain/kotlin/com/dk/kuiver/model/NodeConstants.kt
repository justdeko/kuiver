package com.dk.kuiver.model

import androidx.compose.ui.unit.dp

/**
 * Default node size, used when dimensions are not specified or measured.
 * Used across layout algorithms, rendering, and bounds calculations.
 */
internal val DEFAULT_NODE_SIZE = 60.dp
